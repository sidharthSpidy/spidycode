import { BriefcaseBusiness, MapPin } from "lucide-react";
import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import { ApplyForm } from "@/components/career/forms";
import { getJobs } from "@/features/career/data-access";
export default async function JobsPage() { const jobs = await getJobs(); return <DashboardShell><section className="mx-auto max-w-4xl"><p className="font-mono text-xs text-cyan-200">OPPORTUNITIES</p><h1 className="mt-2 text-3xl font-bold tracking-[-.05em]">Find a role worth building toward.</h1><div className="mt-8 grid gap-4">{jobs.map((job) => <article key={job.id} className="rounded-2xl border border-white/10 bg-white/[.035] p-6"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-lg font-semibold">{job.title}</h2><p className="mt-1 text-sm text-cyan-100">{job.company}</p></div><span className="flex h-fit items-center gap-1 text-xs text-white/50"><MapPin className="size-3.5" />{job.remote ? "Remote" : job.location ?? "On site"}</span></div><p className="mt-4 text-sm leading-6 text-white/55">{job.description}</p><ApplyForm jobId={job.id} /></article>)}{!jobs.length && <div className="rounded-2xl border border-white/10 p-10 text-center text-sm text-white/50"><BriefcaseBusiness className="mx-auto mb-3 size-6" />No roles are open right now.</div>}</div></section></DashboardShell>; }
