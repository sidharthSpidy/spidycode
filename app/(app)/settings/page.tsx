import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import { getCurrentProfile } from "@/lib/auth/dal";
import { SoundToggle } from "@/components/ui/sound-toggle";
import { PasswordForm } from "@/components/auth/password-form";
export default async function SettingsPage() { const profile = await getCurrentProfile(); return <DashboardShell><section className="mx-auto max-w-2xl"><p className="font-mono text-xs text-cyan-200">SETTINGS</p><h1 className="mt-2 text-3xl font-bold tracking-tight">Account settings</h1><div className="mt-7 grid gap-4"><article className="rounded-2xl border border-white/10 bg-white/[.035] p-6"><h2 className="font-semibold">Account</h2><p className="mt-2 text-sm text-white/55">Update your public profile from the Profile page, or secure your account below.</p><PasswordForm /></article><article className="rounded-2xl border border-white/10 bg-white/[.035] p-3"><SoundToggle /></article><article className="rounded-2xl border border-white/10 bg-white/[.035] p-6"><h2 className="font-semibold">Role</h2><p className="mt-2 text-sm capitalize text-white/55">{profile?.role ?? "learner"}</p><p className="mt-1 text-xs text-white/40">Recruiter roles are verified by the SpidyCode team before job publishing is enabled.</p></article></div></section></DashboardShell>; }
