import Link from "next/link";
import { Flame, Medal, Target, Trophy } from "lucide-react";
import { getCurrentProfile } from "@/lib/auth/dal";
import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import { OnboardingForm } from "@/components/auth/onboarding-form";
import { getDashboardLearningData } from "@/features/roadmaps/data-access";
import { AnimatedStat } from "@/components/dashboard/animated-stat";

export default async function DashboardPage() {
  const profile = await getCurrentProfile();
  if (!profile) return <DashboardShell><OnboardingPrompt /></DashboardShell>;
  const learning = await getDashboardLearningData();
  const stats = [{ label: "Rank points", value: profile.xp, icon: Trophy, color: "text-violet-200" }, { label: "Current level", value: profile.level, icon: Medal, color: "text-amber-200" }, { label: "Daily streak", value: profile.streak, suffix: " days", icon: Flame, color: "text-pink-200" }, { label: "Weekly goal", value: 0, suffix: " / 5", icon: Target, color: "text-cyan-200" }];
  const enrolled = learning.enrolled as unknown as Array<{ roadmaps: { id: string; slug: string; title: string; icon: string } | null }>;
  const achievements = learning.achievements as unknown as Array<{ earned_at: string; achievement_definitions: { title: string; description: string; icon: string } | null }>;
  return <DashboardShell><section><p className="font-mono text-xs text-cyan-200">OVERVIEW</p><h1 className="mt-2 text-3xl font-bold tracking-[-.05em] sm:text-4xl">Good evening, {profile.name.split(" ")[0]}.</h1><p className="mt-2 text-sm text-white/55">Your next project is waiting.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ label, value, suffix, icon, color }) => <AnimatedStat key={label} label={label} value={value} suffix={suffix} icon={icon} color={color} />)}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.35fr_.65fr]"><article className="rounded-2xl border border-white/10 bg-white/[.035] p-6"><div className="flex items-start justify-between gap-4"><div><p className="font-mono text-[11px] text-violet-200">CONTINUE LEARNING</p><h2 className="mt-2 text-lg font-semibold">{enrolled.length ? "Continue building momentum" : "Choose your first roadmap"}</h2><p className="mt-2 max-w-md text-sm leading-6 text-white/55">{enrolled.length ? "Your active learning paths are ready whenever you are." : "Start with a guided path, then build something real at the end of each level."}</p></div><span className="rounded-lg bg-violet-300/10 px-2 py-1 font-mono text-[10px] text-violet-100">{enrolled.length ? `${enrolled.length} ACTIVE` : "NEW"}</span></div>{enrolled.length ? <div className="mt-6 grid gap-2 sm:grid-cols-2">{enrolled.filter((item) => item.roadmaps).map(({ roadmaps }) => <Link key={roadmaps!.id} href={`/roadmaps/${roadmaps!.slug}`} className="flex items-center gap-3 rounded-xl border border-white/10 bg-black/15 p-3 text-sm font-medium transition hover:-translate-y-0.5 hover:bg-white/5"><span className="grid size-8 place-items-center rounded-lg bg-violet-300/10 font-mono text-xs text-violet-100">{roadmaps!.icon}</span>{roadmaps!.title}<span className="ml-auto text-white/35">→</span></Link>)}</div> : <Link href="/roadmaps" className="mt-6 inline-flex rounded-full bg-white px-4 py-2.5 text-sm font-bold text-black transition hover:-translate-y-0.5">Explore roadmaps →</Link>}</article><article className="rounded-2xl border border-white/10 bg-white/[.035] p-6"><p className="font-mono text-[11px] text-cyan-200">ACHIEVEMENTS</p>{achievements.length ? <div className="mt-4 space-y-3">{achievements.map(({ achievement_definitions: achievement }) => achievement && <div key={achievement.title} className="flex gap-3"><span className="grid size-8 place-items-center rounded-lg bg-cyan-300/10">{achievement.icon}</span><div><p className="text-sm font-medium">{achievement.title}</p><p className="text-xs text-white/45">{achievement.description}</p></div></div>)}</div> : <><p className="mt-3 text-sm leading-6 text-white/55">Complete your first level to unlock your first achievement.</p><div className="mt-7 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full w-0 rounded-full bg-cyan-300" /></div><p className="mt-2 text-xs text-white/40">Your journey starts with one level.</p></>}</article></div></section></DashboardShell>;
}

function OnboardingPrompt() { return <section className="mx-auto max-w-xl py-16"><p className="font-mono text-xs text-cyan-200">ONE LAST STEP</p><h1 className="mt-3 text-4xl font-bold tracking-[-.055em]">Make your profile yours.</h1><p className="mt-4 text-sm leading-6 text-white/60">Choose a name and username. Your profile is where your projects and progress will live.</p><OnboardingFormWrapper /></section>; }
async function OnboardingFormWrapper() { const { requireUser } = await import("@/lib/auth/dal"); const user = await requireUser(); return <OnboardingForm defaultName={user.user_metadata.full_name ?? user.email?.split("@")[0] ?? ""} />; }
